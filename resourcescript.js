/*
let resourceHTML = ""

resources.map(resource => {
    let sourcesList = ""; 

    for (let i = 0; i < resource.sources.length; i++) {
        sourceList += `<li><a href="${resource.sources[i].url}">${resource.sources[i].title}</a></li>`;
    }

    resourceHTML += `<article>
        <h2>${resource.category}</h2>
        <p>${resource.text}</p>
        <ul>${sourceList}</ul>
    </article>`;
});

const main = document.getElementsByTagName("main")
main[0].innerHTML = resourceHTML

*/
